# دَعمة — نظام التصميم

> الاتجاه: **«دقّة وحضور»**
> الرقيّ يأتي من المساحة والتفاصيل الشعرية، لا من كثافة اللون.
> الأحمر علامة ترقيم لا خلفية.

هذا الملف هو المرجع الذي تُبنى عليه كل الأقسام. مصدر الحقيقة للقيم هو
[`src/styles/globals.css`](src/styles/globals.css) — ما هنا شرحٌ للقواعد،
لا نسخة ثانية منها.

---

## 1. القاعدة الحاكمة

ثلاث قواعد تفسّر كل قرار في الموقع. أي عنصر جديد يجب أن يمرّ عليها:

1. **لا لون مكتوب بالثابت في أي مكوّن.** كل لون يشير إلى متغيّر. هذا وحده
   هو ما جعل الوضع الداكن يعمل دون لمس مكوّن واحد.
2. **الخطّ الشعري (1px) هو وحدة البناء** — لا كروت، ولا ظلال، ولا حدود
   سميكة. الفصل بين الأشياء يتمّ بخطّ أو بمساحة.
3. **الأحمر لا يملأ مساحة.** يظهر كنقطة، أو رقم، أو حدّ عند التفاعل. لو
   احتجت مساحة حمراء كبيرة، فالتصميم أخطأ في مكان آخر.

---

## 2. اللون

الأسماء دلالية لا وصفية: **«الورق» هو الخلفية دائمًا و«الحبر» هو النص
دائمًا** — أيًّا كان لونهما. لهذا يعمل `bg-paper text-ink` صحيحًا في
الوضعين دون أي شرط.

| الرمز | فاتح | داكن | الاستعمال |
|---|---|---|---|
| `--color-paper` | `#FBFAF8` | `#0C0C0E` | الخلفية |
| `--color-paper-2` | `#F2EFE9` | `#141417` | سطح مرتفع قليلًا |
| `--color-paper-3` | `#E8E4DC` | `#1E1E22` | سطح ثالث، شريط التمرير |
| `--color-ink` | `#0B0B0D` | `#EDEBE7` | النص الأساسي |
| `--color-red` | `#E70000` | `#FF1A1A` | اللكنة فقط |
| `--line` | `ink 10%` | `ink 13%` | الخطّ الشعري القياسي |
| `--line-strong` | `ink 18%` | `ink 24%` | حدّ زرّ، حالة hover |
| `--line-faint` | `ink 5%` | `ink 7%` | شبكة الخلفية |
| `--muted` | `ink 56%` | `ink 55%` | نص ثانوي |

**لماذا ليست ألوانًا صافية:** الأبيض الصافي يبدو معمليًا والأسود الصافي
يبدو حفرة. الورق الدافئ والحبر شبه الأسود يعطيان عمقًا دون أن يلفتا النظر.

**لماذا يتغيّر الأحمر:** `#E70000` على خلفية شبه سوداء يفقد تشبّعه ويُقرأ
بنّيًا. النسخة الداكنة مرفوعة في الإضاءة فقط — الدرجة اللونية (0°) كما هي،
فهو نفس أحمر الهوية.

### نسبة اللون
الأحمر يجب ألّا يتجاوز **~2% من مساحة الشاشة**. لو تجاوزها فقد قيمته
كإشارة وصار خلفية.

---

## 3. الطباعة

عائلتان تتقاسمان الهيكل، ومكدّس واحد لكل الموقع:

```
"Satoshi", "Plex Arabic", system-ui, sans-serif
```

نطاق `Plex Arabic` محصور بالعربي عبر `unicode-range`، فالمتصفح يختار الخط
المناسب **لكل حرف** تلقائيًا. لا تبديل يدوي، ولا فئة `.font-ar`.

| الفئة | الوظيفة | ملاحظة |
|---|---|---|
| `.display` | عنوان الهيرو | الوزن 500 لاتيني / 600 عربي |
| `.lede` | جملة البيان | الوزن 300 لاتيني / 400 عربي |
| `.h2` | عنوان قسم | |
| `.body` | نص الجسم | لونه `--muted` تلقائيًا |
| `.tag` | لافتة صغيرة | حروف كبيرة وتتبّع واسع لاتينيًا، طبيعية عربيًا |

### القاعدة الحرجة
**لا تتبّع سالب (`letter-spacing`) على العربي — أبدًا.** يكسر وصلات الحروف.
كل ضبط للتتبّع مشروط بـ `html[lang="en"]`. والعربي يأخذ ارتفاع سطر أكبر
(1.2–2.05 مقابل 1.0–1.7 للاتيني) لأن التشكيل والنقاط تحتاج مجالًا رأسيًا.

الأرقام تأخذ `.nums` (`tabular-nums`) في أي مكان تتغيّر فيه — وإلا رقصت
أثناء العدّ.

---

## 4. التخطيط

- **الحاوية:** `1320px` كحدّ أقصى، هوامش `24px` / `40px` من `sm`.
- **الشبكة:** 12 عمودًا. تُستعمل `lg:col-span-*` و`lg:col-start-*` فقط من
  مقاس `lg`؛ ما دون ذلك تكديس رأسي.
- **مساحة القسم:** `py-28 sm:py-36 lg:py-44`.
- **الاتجاه:** خصائص منطقية حصرًا (`ps`/`pe`، `ms`/`me`، `start`/`end`،
  `border-s`/`border-e`). لا `left`/`right` في أي مكوّن جديد.

> **مطبّة معروفة:** الخصائص المنطقية على عنصر ذي `writing-mode` رأسي
> تُحسب بنمط كتابة العنصر نفسه فينقلب المحور. ضع التموضع على غلاف أفقي
> — كما في التاجلاين الرأسي في [`Hero.tsx`](src/sections/Hero.tsx).

### شبكة الخطوط الشعرية
الحاوية ترسم حدّ **الأعلى والبداية**، وكل خانة ترسم حدّ **الأسفل والنهاية**.
بهذا تكتمل الشبكة دون خطوط مزدوجة ولا ناقصة مهما تغيّر عدد الأعمدة بين
المقاسات — بلا حيل `nth-child` تنكسر عند كل نقطة توقّف.

### مطبّات Tailwind التي كلّفتنا وقتًا

| المشكلة | الصواب |
|---|---|
| `duration-[var(--dur-base)]` يُنتج `0s` بصمت — القيمة العشوائية تُتحقَّق كـ `<time>` و`var()` لا يطابقها | `duration-(--dur-base)` — اختصار متغيّرات CSS في v4 |
| `hidden` على مكوّن يثبّت `inline-flex` لا يُخفيه: أدوات `display` تتغلّب بترتيب الملف لا بترتيب الفئات | أخفِ عبر غلاف: `<span className="hidden md:block">` |
| قاعدة CSS خارج طبقات Tailwind تحمل `transition` مختصرة تُلغي `transition-colors` | اذكر كل الخصائص في القاعدة نفسها |

---

## 5. الحركة

### منحنيان لا ثالث لهما

| الرمز | القيمة | متى |
|---|---|---|
| `--ease-out-quint` | `cubic-bezier(0.16, 1, 0.3, 1)` | كل ما يدخل أو يُكشف |
| `--ease-in-out` | `cubic-bezier(0.65, 0, 0.35, 1)` | التبديلات ثنائية الاتجاه فقط |

تعدّد المنحنيات هو ما يجعل المواقع تبدو مُجمّعة من قطع.

### سلّم المدد

| الرمز | المدّة | الوظيفة |
|---|---|---|
| `--dur-fast` | `200ms` | ردّ فوري: لون، حدّ |
| `--dur-base` | `320ms` | تفاعل: hover، تبديل، انزلاق سهم |
| `--dur-slow` | `600ms` | انتقال عنصر كامل، ستارة الدخول |
| `--dur-reveal` | `900ms` | كشف عند دخول الشاشة |
| `--dur-hero` | `1100ms` | سطور العنوان الضخم |

### طبقات الحركة الثلاث

1. **لحظة الدخول** — ثلاثة شيفرونات تدخل تباعًا ثم ترتفع الستارة. أول
   زيارة فقط (`localStorage` بمهلة 12 ساعة، ثابت في
   [`Intro.tsx`](src/components/Intro.tsx)).
2. **الكشف عند التمرير** — `<Reveal>` للكتل، و`<MaskLines>` للعناوين.
3. **طبقة اللمس** — `.arrow-swap`، وخطّ `.ulink`، وانزياح السطور، وتشبّع
   صور الأعمال.

### القواعد
- **التخطيط لا يعتمد على الحركة أبدًا.** البنية (`display`، `overflow`)
  خارج `.anim-ready` دائمًا؛ داخلها التحويلات والشفافية فقط. المحتوى ظاهر
  افتراضيًا حتى لو لم تُشغَّل أي حركة.
- **لا تحرّك `grid-template-rows` بين `0fr` و`1fr`.** هي الحيلة الشائعة
  لتحريك ارتفاع مجهول بلا JS، لكن المتصفح لا يُنهي استيفاء `fr` في حاوية
  بارتفاع غير محدّد فتبقى القيمة `0px` والمحتوى لا يظهر إطلاقًا. قِس
  الارتفاع بالبكسل و`ResizeObserver` — كما في [`Faq.tsx`](src/sections/Faq.tsx).
- **الشريط بسرعة ثابتة** — لا ارتباط بسرعة التمرير.
- **`prefers-reduced-motion` يُلغي** لحظة الدخول وعدّ الأرقام وتلاشي السمة.
- **بوّابة الحركة** (`MotionGate`) تؤجّل كل كشف إلى ما بعد ارتفاع الستارة،
  وإلا انتهت حركة الهيرو خلفها.

---

## 6. الصفحات والبنية

```
src/
  pages/      صفحة لكل مسار
  sections/   أقسام الرئيسية — ملخّصات تربط بالصفحات الكاملة
  components/ الهيدر والفوتر وعناصر البناء المشتركة
  content/    home.ts (الرئيسية) · pages.ts (الصفحات الداخلية)
```

| المسار | الصفحة |
|---|---|
| `/` | الرئيسية — ملخّص كل قسم |
| `/about` | الرؤية، الرسالة، المشكلة/الحل، لماذا دَعمة، الأرقام |
| `/services` | الخدمات الستّ كصفوف |
| `/services/:slug` | ما تشمله الخدمة، ما تستلمه، أعمال مرتبطة |
| `/work` | كل الأعمال بمرشّحات |
| `/work/:slug` | حالة دراسية: الوصف، البيانات، الصورة، النتائج |
| `/process` | الخطوات الخمس بمدّة كل خطوة ومخرجاتها |
| `/packages` | الباقات + أسئلة التسعير |
| `/contact` | القنوات المباشرة والتفاصيل |
| `*` | 404 |

**الرئيسية ملخّص لا نسخة.** كل قسم فيها ينتهي برابط إلى صفحته الكاملة،
والتفصيل يعيش في الصفحة وحدها. تكرار المحتوى بين الاثنين يضرّ بالسيو
ويضاعف كلفة أي تعديل.

**التنقّل داخلي:** `SmartLink` يختار `Link` للمسارات الداخلية و`<a>`
للمراسي والروابط الخارجية، فلا تُعاد الصفحة بالكامل عند كل نقرة.
`ScrollManager` يعيد التمرير إلى الأعلى عند تغيّر المسار — المتصفح لا
يفعلها وحده في تطبيق ذي راوتر داخلي.

### اللغة في المسار

```
/            العربية        /en           الإنجليزية
/about                      /en/about
/work/amlak                 /en/work/amlak
```

**اللغة تأتي من المسار، لا من التخزين المحلي.** كانت محفوظة في
`localStorage` والنسختان على رابط واحد — أي أن الزاحف لا يرى إلا
العربية، ونصف المحتوى غير موجود لمحركات البحث. الآن لكل لغة رابطها،
و`hreflang` يربطهما.

المكوّنات تكتب المسار **مجرّدًا** دائمًا (`/contact`)، والبادئة تُضاف في
`SmartLink` و`useLang().path()` وحدهما. لو أضافها مكوّن بنفسه، قفز زائر
النسخة الإنجليزية إلى العربية عند أول نقرة.

### الرأس والفهرسة

`<Seo>` يضبط لكل مسار: العنوان، الوصف، `canonical`، `hreflang`،
ووسوم OG وTwitter. قبلها كان `canonical` ثابتًا على الرئيسية في كل
صفحة — أي أننا نخبر جوجل أن الصفحات كلها نسخة مكرّرة من الرئيسية.

المصدر الوحيد لبيانات المسارات هو
[`src/content/seo.ts`](src/content/seo.ts)، يُقرأ مرّتين: من التطبيق وقت
التشغيل، ومن إضافة البناء في [`vite.config.ts`](vite.config.ts).

### لماذا نولّد HTML ثابتًا لكل مسار

زاحفات الشبكات الاجتماعية (واتساب، فيسبوك، لينكدإن) **لا تشغّل
JavaScript**. لو بقيت الوسوم تُكتب وقت التشغيل فقط، لظهرت كل روابط
الموقع بعنوان الرئيسية وبلا صورة معاينة.

فبعد `vite build` تكتب الإضافة ملف `index.html` لكل مسار × لغة (58 ملفًا)
برأسٍ صحيح، مع `sitemap.xml` و`robots.txt`. الجسم يبقى غلاف التطبيق —
جوجل يشغّل JavaScript ويرى المحتوى.

**النشر:** المسارات من طرف العميل، فالخادم يجب أن يعيد `index.html` لأي
مسار. مضبوط في [`public/_redirects`](public/_redirects) لـ Netlify
و[`vercel.json`](vercel.json) لـ Vercel. بدونها تعمل الروابط من داخل
الموقع وتعطي 404 عند فتحها مباشرةً.

## 7. الأقسام

| # | القسم | الخلفية | الملاحظة |
|---|---|---|---|
| — | الهيرو | ورق | شبكة رأسية + شريط أعمال متحرّك بسرعة ثابتة |
| 01 | من نحن | ورق | بيان + ثلاث ركائز + أرقام |
| 02 | خدماتنا | ورق-2 | شبكة خطوط شعرية 3×2 |
| 03 | أعمالنا | ورق | مرشّحات + شبكة صور رمادية تتلوّن عند المرور |
| 04 | كيف نعمل | **حبر** | القسم الوحيد المقلوب — يقلب معه في الوضعين |
| 05 | باقاتنا | ورق | ثلاثة أعمدة بلا أسعار — الاسم هو المرساة، والنداء «اطلب عرض سعر» |
| 06 | أسئلة متكررة | ورق-2 | أكورديون، واحد مفتوح في كل مرة |
| 07 | تواصل معنا | ورق | قنوات مباشرة لا نموذج معطّل |
| — | الفوتر | **حبر** | |

القسم المقلوب (`bg-ink text-paper`) ينعكس تلقائيًا مع السمة: داكن على موقع
فاتح، وفاتح على موقع داكن. حدوده تستعمل `border-paper/12` لا `--line`،
لأن متغيّر الخطّ العام محسوب لخلفية القسم لا للمقلوبة.

## 8. المكوّنات

الموجود في [`src/components/ui.tsx`](src/components/ui.tsx):

| المكوّن | الوظيفة |
|---|---|
| `Wrap` | الحاوية |
| `Reveal` | كشف كتلة عند دخول الشاشة |
| `MaskLines` | عنوان تصعد سطوره من خلف قناع؛ `accentDot` يلوّن النقطة الأخيرة |
| `Btn` | زرّ كبسولي — `ink` / `outline` / `paper` |
| `TextLink` | رابط نصّي بخطّ يُرسم تحته |
| `SectionLabel` | `(01) —— اسم القسم` |
| `Counter` | رقم يعدّ عند ظهوره |
| `Chevron` | موتيف `»` |
| `ArrowSwap` | سهمان في نافذة: الأول يخرج والثاني يدخل |
| `Arrow` | سهم ينعكس تلقائيًا في RTL |

### حركة الزرّ
اللون **يدخل كمسحة لها اتجاه**، لا كتغيّر لون دفعةً واحدة — الأخير مسطّح
ولا يوحي بأن الزرّ استجاب.

الحيلة في نقطة الأصل: وهو ساكن أصلها جهة **الخروج**، وعند المرور تنتقل
إلى جهة **الدخول**. فتدخل المسحة من جهة القراءة وتخرج من الجهة المقابلة،
ولا ترجع أدراجها كشريط يُلَفّ للخلف. كل ذلك في CSS خالص بلا حالة.

يرافقها `.arrow-swap` وانكماش خفيف عند الضغط (`:active`) — الأخير هو ما
يجعل الزرّ جسمًا يُضغط لا صورة تُنقر.

لون المسحة يأتي من `--btn-fill` لكل نمط، فيبقى المكوّن واحدًا.

### النصّ داخل الأحمر
`text-paper` ينقلب إلى داكن في الوضع الداكن. أي نصّ فوق خلفية حمراء يأخذ
`text-white` صريحًا.

---

## 9. المحتوى ثنائي اللغة

لا نصّ ثابت داخل أي مكوّن. كل شيء يمرّ عبر
[`src/content/home.ts`](src/content/home.ts) بالشكل:

```ts
{ ar: "…", en: "…" }
```

ويُقرأ بـ `const { t } = useLang()` ثم `t(x)`. الإنجليزية تُكتب بإيقاعها
لا كترجمة حرفية — كسور السطور في العناوين مقصودة ومكتوبة يدويًا لكل لغة.

اللغة والسمة تُطبَّقان في `<head>` **قبل أول رسم**، وإلا ومض الاتجاه من
RTL إلى LTR وومضت الخلفية البيضاء قبل الداكنة.

---

## 10. الوصول

- حلقة تركيز حمراء عبر `:focus-visible` على كل عنصر تفاعلي.
- القوائم المخفية تأخذ `inert` — الشفافية وحدها تُبقي الروابط في مسار
  لوحة المفاتيح.
- الصور الزخرفية `aria-hidden`، والوظيفية لها `alt` مترجَم.
- كل زرّ أيقوني له `aria-label` مترجَم، وحالته في `aria-pressed`.

---

## 11. قائمة فحص القسم الجديد

- [ ] لا لون مكتوب بالثابت — كل شيء عبر متغيّر
- [ ] خصائص منطقية فقط، ومُختبر في الاتجاهين
- [ ] نصوصه في `home.ts` بالعربي والإنجليزي
- [ ] مُختبر في الوضعين الفاتح والداكن
- [ ] مدده من السلّم، ومنحناه `--ease-out-quint`
- [ ] تخطيطه سليم دون `.anim-ready`
- [ ] الأحمر لم يتجاوز نسبته
- [ ] الصور الفاتحة مخفّفة في الداكن (`dark:brightness-*`)
